<!DOCTYPE html>
<html lang="en">
  <%- include('../../layout/head') %>

<body>
  <%- include('../../layout/header') %>
    <main>
        <h1>Details book</h1>
        <ul>
                <li>
                    <img src=" <%= results[0].image %>" alt="">
                  <h4>
                    <%= results[0].title %>
                  </h4>
                  <p>
                    <%= results[0].author %>
                  </p>
                  <p><%= results[0].description %></p>
                  <p><%= results[0].isbn %></p>
                </li>
                  </ul>
                  <button onclick="disForm()">Update Details</button>
                  <form action="/books/<%= results[0].id%>?_method=DELETE" method="POST">
                    <input type="hidden" value="<%= results[0].id%>">                    
                    <input type="submit" value="delete this Book">
                  </form>
    </main>
    <%- include('edit') %>
    <script src="../js/app.js"></script>
</body>
</html>